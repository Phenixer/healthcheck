test-runner-service:
  build:
    image: test-runner-image
    dockerfile: Dockerfile
  cached: true
  depends_on:
    - postgres11
postgres11:
  build:
    image: postgres11
    context: docker/postgres11
    dockerfile: Dockerfile
  cached: true
  environment:
    POSTGRES_PASSWORD: password