mysql:
  build:
    image: mysql
    context: mysql
    dockerfile: Dockerfile
  depends_on: postgres
  cached: true
  environment:
    MYSQL_ROOT_PASSWORD: password
postgres:
  build:
    image: postgres
    context: postgres
    dockerfile: Dockerfile
  cached: true
  environment:
    POSTGRES_PASSWORD: password